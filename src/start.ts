// import { Server } from "@/main";

// declare const module: any;

// const server = new Server()

// server.bootstrap()

// server.attachMiddleware()

// if (module.hot) {
//     module.hot.accept();
//     module.hot.dispose(() => server.app.close());
// }